# Components

This directory contains reusable UI components for the TrustMetrics application.

## Structure

- Place shared components here
- Use subdirectories to organize related components
- Each component should be in its own file or directory

## Example

```
components/
  ├── Button.tsx
  ├── Card.tsx
  └── forms/
      └── Input.tsx
```
